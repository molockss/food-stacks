<%- include('partials/header') -%>
    <main class="container card-container">
        <div class="row justify-content-center mb-4 space-div">
            <section class="col-6 mb-5 space login_input">
              <h1>Sign up with email</h1><br>
                <% if (locals.messages.errors) { %>
                    <% messages.errors.forEach( el => { %>
                        <div class="alert alert-danger"><%= el.msg %></div>
                    <% }) %>    
                <% } %>
                <% if (locals.messages.info) { %>
                    <% messages.info.forEach( el => { %>
                        <div class="alert alert-info"><%= el.msg %></div>
                    <% }) %>    
                <% } %>
                <form action="/signup" method="POST">
                    <div class="mb-3 login_input">
                        <label for="userName" class="form-label">User Name</label>
                        <input type="text" class="form-control" id="userName" name="userName">
                      </div>
                    <div class="mb-3 login_input">
                      <label for="exampleInputEmail1" class="form-label">Email address</label>
                      <input type="email" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" name="email">
                      <div id="emailHelp" class="form-text">We'll never share your email with anyone else.</div>
                    </div>
                    <div class="mb-3 login_input">
                      <label for="password" class="form-label">Password</label>
                      <input type="password" class="form-control" id="password" name="password">
                    </div>
                    <div class="mb-3 login_input">
                        <label for="confirmPassword" class="form-label">Confirm Password</label>
                        <input type="password" class="form-control" id="confirmPassword" name="confirmPassword">
                      </div>
                    <button type="submit" class="btn btn-primary">Create my account</button>
                    
                  </form>

                  <form action="/login" method="POST">
                    <div>
                        <label for="email" class="left"></label>
                        <input type="email" name="email" id="email" value="account@demo.com" hidden/>
                        <label for="password" class="left"></label>
                        <input type="password" name="password" id="password" value="accountdemo123" hidden/>
                    </div>
                    <div>
                        <button type="submit" class="btn btn-success">Explore a demo account (No Sign-up required)</button>
                    </div>
                </form>
            </section>
        </div>
        
    </main>
    

<%- include('partials/footer') -%>